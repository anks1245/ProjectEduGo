<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.example.projecthackathon">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".MentorAppointment"/>
        <activity android:name=".mentor.MentorActivity" />
        <activity android:name=".course.CourseFullActivity" />
        <activity android:name=".job.JobFullDescription" />
        <activity android:name=".job.JobsAndInternshipActivity" />
        <activity android:name=".course.CourseActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Main" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignInUpActivity" />
    </application>

</manifest>